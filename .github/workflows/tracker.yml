name: Compact Phone Tracker

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - run: python tracker.py
        env:
          BOT_TOKEN: ${{ 8715119832:AAGDoOkiHjeKZFMa9yT6t1oLo8k3G4AvRZc }}
          CHAT_ID: ${{ 5654462790 }}
